# pnpm configuration for NestJS monorepo
# shamefully-hoist exposes nested dependencies at root node_modules,
# required because NestJS relies on flat module resolution for decorators.
shamefully-hoist=true
strict-peer-dependencies=false
